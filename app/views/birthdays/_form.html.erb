<%= form_with(model: birthday, local: true) do |form| %>
  <% if birthday.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(birthday.errors.count, "error") %> prohibited this birthday from being saved:</h2>

      <ul>
      <% birthday.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :birthday_nombre %>
  </div>

  <div class="field">
    <%= form.label :fecha %>
    <%= form.datetime_select :fecha, id: :birthday_fecha %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
